<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.Resposty.AccountRepository"><!--Repository接口的地址-->
    <!-- <cache></cache>&lt;!&ndash;自带方式的mapper开启二级缓存&ndash;&gt;-->
    <cache type="org.mybatis.caches.ehcache.EhcacheCache">
        <!--创建后，最后一次访问缓存的时间至缓存失效的时间间隔-->
        <property name="timeToIdleSeconds" value="3600"/>
        <!--缓存自创建时间起至失效时间间隔-->
        <property name="timeToLiveSeconds" value="3600"/>
        <!--缓存回收策略 LRU表示移除近期使用最少的对象-->
        <property name="memoryStoreEvictionPolicy" value="LRU"/>
    </cache>
    <insert id="save" parameterType="org.example.Entiy.Account"> /*id 调用方法用到的参数 type参数的数据类型*/
        insert into t_account(username,password,age) value (#{username},#{password},#{age})
    </insert>
    <update id="update" parameterType="org.example.Entiy.Account">
        update t_account
        <set>
            <if test="username!=null">
                username=#{username}
            </if>
            <if test="password!=null">
                password=#{password}
            </if>
            <if test="age!=0">
                age=#{age}
            </if>
        </set>
        where id=#{id}
    </update>
    <delete id="deleteById" parameterType="long">
        delete from t_account where id=#{id}
    </delete>
    <select id="findAll" resultType="org.example.Entiy.Account">
        select * from t_account;
    </select>
    <select id="findById" parameterType="long" resultType="org.example.Entiy.Account">
        select * from t_account where id=#{id};
    </select>
    <select id="findByNameAndAge" resultType="org.example.Entiy.Account">
        select * from t_account where name=#{param1} and age=#{param2};
    </select>
    <select id="findByAccount" parameterType="org.example.Entiy.Account" resultType="org.example.Entiy.Account">
        <!-- select * from t_account
         <where>
             <if test="id!=0">
                 id=#{id}
             </if>
             <if test="username!=null">
                 and username=#{username}
             </if>
             <if test="password!=null">
                 and password=#{password}
             </if>
             <if test="age!=0">
                 and age=#{age}
             </if>
         </where>-->
        <!-- select * from t_account
         <where>
             <choose>
                 <when test="id!=0">
                     id=#{id}
                 </when>
                 <when test="username!=null">
                     username=#{username}
                 </when>
                 <when test="password!=null">
                     password=#{password}
                 </when>
                 <when test="age!=0">
                     age=#{age}
                 </when>
             </choose>
         </where>-->
        select * from t_account
        <trim prefix="where" prefixOverrides="and">
            <if test="id!=0">
                id=#{id}
            </if>
            <if test="username!=null">
                and username=#{username}
            </if>
            <if test="password!=null">
                and password=#{password}
            </if>
            <if test="age!=0">
                and age=#{age}
            </if>
        </trim>
    </select>
    <select id="findByids" parameterType="org.example.Entiy.Account" resultType="org.example.Entiy.Account">
        select * from t_account
        <where>
            <foreach collection="ids" open="id in (" close=")" item="id" separator=",">
                #{id}
            </foreach>
        </where>
    </select>
</mapper>