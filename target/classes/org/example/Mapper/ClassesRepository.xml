<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.Resposty.ClassesRepository">
    <resultMap id="ClassesMap" type="org.example.Entiy.Classes">
        <id column="id" property="id"></id>
        <result column="name" property="name"></result>
        <collection property="students" ofType="org.example.Entiy.Student">
            <id column="sid" property="id"></id>
            <result column="sname" property="name"></result>
        </collection>
    </resultMap>
    <select id="findById" parameterType="long" resultMap="ClassesMap">/*按照班级查班级所有学生*/
        select s.id as sid,s.name as sname,c.id,c.name from student s,classes c where c.id=#{id} and s.cid=c.id;
    </select>

    <select id="findByIdLazy" parameterType="long" resultType="org.example.Entiy.Classes">/*按照班级查班级所有学生*/
        select * from classes where id=#{id};
    </select>
</mapper>